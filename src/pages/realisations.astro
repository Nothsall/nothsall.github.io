<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>À propos</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blanc">
    <nav id="navbar" class="text-noir">
        <div class="max-w-6xl mx-auto px-4">
            <div class="flex justify-between items-center h-16">
                <div class="flex-1 flex justify-center space-x-7 mt-1">
                    <div class="hidden md:flex items-center space-x-8 fixed">
                        <a href="#a_propos" class=" px-2 font-semibold transition duration-300 font-titre scroll-link">À PROPOS</a>
                        <a href="#competences" class=" px-2 font-semibold transition duration-300 font-titre scroll-link">COMPÉTENCES</a>
                        <a href="#realisations" class=" px-2 font-semibold transition duration-300 font-titre scroll-link">RÉALISATIONS</a>
                        <a href="#contact" class=" px-2 font-semibold transition duration-300 font-titre">CONTACT</a>
                    </div>
                </div>
                <div class="md:hidden flex items-center justify-end">
                    <button class="outline-none mobile-menu-button">
                        <svg class="w-10 h-10 text-noir" fill="none" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" stroke="currentColor">
                            <path d="M4 6h16M4 12h16m-7 6h7"></path>
                        </svg>
                    </button>
                </div>
            </div>
        </div>
    
        <div class="hidden mobile-menu">
            <ul class="">
                <li><a href="#a_propos" class="block text-sm px-2 py-4 text-noir hover:bg-gray-200 transition duration-300 font-titre scroll-link">À PROPOS</a></li>
                <li><a href="#competences" class="block text-sm px-2 py-4 text-noir hover:bg-gray-200 transition duration-300 font-titre scroll-link">COMPÉTENCES</a></li>
                <li><a href="#realisations" class="block text-sm px-2 py-4 text-noir hover:bg-gray-200 transition duration-300 font-titre scroll-link">RÉALISATIONS</a></li>
                <li><a href="#contact" class="block text-sm px-2 py-4 text-noir hover:bg-gray-200 transition duration-300 font-titre">CONTACT</a></li>
            </ul>
        </div>
    </nav>

    <script>
        const btn = document.querySelector("button.mobile-menu-button");
        const menu = document.querySelector(".mobile-menu");
        if (btn) {
            btn.addEventListener("click", () => {
        if (menu) {
            menu.classList.toggle("hidden");
        }
        });
    }
    </script>
</body>
</html>